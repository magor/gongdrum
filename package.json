{
  "name": "gong-drums-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:galleries": "node scripts/generate-galleries.mjs",
    "dev": "npm run generate:galleries && vite",
    "build": "npm run generate:galleries && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  }
}
